{
  "name": "coderower-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Coderower Assignment project with backend and frontend",
  "author": "Suraj Singh",
  "license": "ISC",
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "dev": "npm run seed && npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

